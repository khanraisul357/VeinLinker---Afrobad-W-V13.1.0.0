spring.application.name=VeinLinker

# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/myapp_db
spring.datasource.username=root
spring.datasource.password=RaisuL777nAbA357khan*

# MySQL driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Flyway auto-runs your V1, V2, V3 SQL files on startup
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Optional but helpful during development
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=validate
